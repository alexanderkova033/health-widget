<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ (API 31+) cloud backup / device-transfer rules. Same rationale as
     backup_rules.xml: only local, non-sensitive settings are persisted. -->
<data-extraction-rules>
    <cloud-backup>
        <include domain="sharedpref" path="." />
    </cloud-backup>
    <device-transfer>
        <include domain="sharedpref" path="." />
    </device-transfer>
</data-extraction-rules>
